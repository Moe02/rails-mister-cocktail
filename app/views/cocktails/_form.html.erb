<%= simple_form_for @cocktail do |f| %>
  <%= f.input :name %>
  <%= f.association :ingredients %>
  <!-- , as: :check_boxes -->
  <%= f.input :photo, as: :file %>
  <%= f.button :submit %>
<% end %>
