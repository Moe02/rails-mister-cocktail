<%= simple_form_for @cocktail do |f| %>
  <%= f.input :name %>
  <%= f.association :ingredients %>
  <!-- , as: :check_boxes -->
  <%#= f.association :description %>
  <%= f.button :submit %>
<% end %>
